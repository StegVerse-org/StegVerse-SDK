# STEGVERSE SDK

This legacy filename is retained only for compatibility with older links.

Use the current canonical documentation instead:

- `README.md` — public SDK overview and governance quick start
- `docs/SDK_CONSOLE.md` — console and navigation reference
- `docs/PUBLIC_INSPECTION_ENTRY.md` — public inspection workflow
- `docs/PUBLIC_CREDENTIAL_BOUNDARY.md` — runtime credential boundary
- `SDK_MIRROR_HANDOFF.md` — repository source of truth

Historical examples in older revisions of this file are not the current SDK contract.
