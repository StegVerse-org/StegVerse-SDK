# ÉLAN × StegVerse Next-Phase Experiment Preregistration

Date: 2026-09-16  
Task: `ELAN-NEXT-PHASE-EXPERIMENT-001`  
State: `FROZEN / WAITING-NEXT-AUTHENTIC-ELAN-SOURCE-NATIVE-OUTPUT`

## Controlling procedure

The canonical procedure for this experiment is:

`docs/ELAN_NEXT_PHASE_EXPERIMENT_PROCEDURE.md`

That procedure controls the next experimental step. This preregistration freezes the evidence fields and interpretation rules before the next authentic ELAN source-native output is examined.

## Preserved predecessor state

Canonical predecessor evidence establishes:

- Run 1 preserved ELAN-native Events 1–2 and stopped at `READY_FOR_GOVERNANCE_CONSUMPTION`.
- Run 2 preserved Event 3 as the previously recorded non-emission event with `emission_observed=false`, intent `UNDETERMINED`, and semantics `UNRESOLVED`.
- Run 2 produced `ALLOW / ok`, custody, deterministic replay, reconstruction, and returned-result evidence.
- The cumulative visual revision was generated and validated but not published.

These predecessor events and results are immutable inputs. They are not restarted, rewritten, or reinterpreted by the next-phase experiment.

## Next observation boundary

The next experimental observation begins only when the next actual source-native output produced by ELAN is available.

The following are not ELAN observations and do not advance the experiment:

- repository silence;
- absence of a GitHub commit;
- missing GitHub evidence;
- elapsed time;
- lack of an attached artifact;
- synthetic ELAN messages;
- inferred ELAN behavior;
- StegVerse-generated replacement observations;
- unrelated runtime evidence;
- unrelated evaluator evidence.

No GitHub Token Authority, third-party evaluator execution, resident substrate, WorkerCoordinator claim, or live StegOS/InTr runtime predicate is required merely to preserve this preregistration or await the next authentic ELAN output.

## Frozen expected-evidence declaration

The following fields are fixed before the next authentic ELAN output is examined:

1. `event_identity_and_order`
2. `observation_boundaries`
3. `exact_source_native_output`
4. `source_native_state`
5. `continuity_relative_to_preceding_non_emission_event`
6. `exact_next_emitted_transition`
7. `provenance_and_custody`
8. `immutable_manifest_reference`
9. `governed_result_reference`
10. `deterministic_replay_result`
11. `reconstruction_result`
12. `returned_result_evidence`

## Interpretation controls

- Capture the next authentic ELAN source-native output exactly before interpretation.
- `NOT_EXPOSED` is used for information ELAN does not expose.
- `UNRESOLVED` is used for semantic questions the available evidence does not resolve.
- Missing source-native fields are never imputed from StegVerse or another architecture.
- No post-hoc evidence field additions may improve, rescue, or reinterpret the observed result.
- Do not infer hidden intent, consent, refusal, withdrawal, emotion, motivation, or other unexposed internal state.

## Governed evidence continuation

After the next authentic ELAN source-native output is captured exactly:

1. bind it to the preserved predecessor evidence, frozen declaration, immutable manifest/reference, and experiment correlation identity;
2. represent only observed information;
3. submit the preserved observation through the established experiment-governance path used by the prior ELAN lineage;
4. preserve the governed-result reference and custody evidence;
5. perform and record deterministic replay;
6. perform and record reconstruction;
7. preserve the returned result exactly;
8. compare the complete evidence set only against the frozen declaration.

Separate runtime or evaluator-validation work remains additive and non-blocking unless this experiment is explicitly amended later.

## Current authentic observation target

`NEXT_AUTHENTIC_ELAN_SOURCE_NATIVE_OUTPUT_AFTER_PREVIOUSLY_RECORDED_NON_EMISSION_EVENT`

Until that authentic output exists, no new ELAN experimental observation is claimed.
